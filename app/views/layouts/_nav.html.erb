<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
  <a routerlink="/" class="navbar-brand" href="/">DevBlog</a>
  <div class="collapse navbar-collapse" id="navbarCollapse">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a routerlink="/" class="nav-link" href="/">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">About</a>
      </li>
    </ul>
    <div class="bmd-form-group bmd-collapse-inline pull-xs-right search-buttom">
      <%= simple_form_for :root_path, method: :get do |f| %>
      <button type="submit" class="btn bmd-btn-icon collapsed search-buttom" for="search" data-toggle="collapse"
        data-target="#collapse-search" aria-expanded="false" aria-controls="collapse-search">
        <span class="material-icons">
          search
        </span>
        <div class="ripple-container search-buttom"></div>
      </button>
      <span id="collapse-search" class="width collapse search-buttom">
        <input class="form-control" type="text" id="search" placeholder="Enter your query..." name="q">
      </span>
      <% end %>
    </div>
    <% if account_signed_in? %>
    <ul class="navbar-nav">
      <li>
        <a routerlink="/posts/new" class="btn btn-primary" href="/posts/new">New Post</a>
      </li>
      <li class="nav-item">
        <%= link_to 'Sign Out', destroy_account_session_path, method: :delete, class: 'nav-link' %>
      </li>
    </ul>
    <% else %>
    <ul class="navbar-nav">
      <li>
        <button type="button" class="btn nav-link active" data-toggle="modal" data-target="#modalLogin">
          Get Started
        </button>
      </li>
    </ul>
    <% end %>
  </div>
</nav>
<script>
  // clear field value once closed
  $(function () {
    $('#collapse-search').on('hidden.bs.collapse', function () {
      $('#search').val('')
    })
  });
</script>
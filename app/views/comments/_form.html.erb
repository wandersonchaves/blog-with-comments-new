<%= simple_form_for([@post, @post.comments.build]) do |f| %>
<!--
  <div class="field">
    <div class="control">
      <%= f.input :name, input_html: { class: "input" }, wrapper: false, label_html: { class: "label" } %>
    </div>
  </div>
  -->
<div class="form-group">
  <div class="control">

    <% if account_signed_in? %>
    <%= f.input :comment, input_html: { class: "form-control" }, wrapper: false, label_html: { class: "label" } %>
    <%= f.button :submit, class: "btn btn-primary" %>
    <% else %>
    <%= f.input :comment, input_html: { class: "form-control", "data-toggle": "modal", "data-target": "#modalLogin" }, wrapper: false, label_html: { class: "label" } %>
    <script>
      // $(function () {
      //   $('#modal-test').on('click', function () {
      //     $('#modalLogin').modal()
      //   })
      // });
    </script>
    <% end %>
  </div>
</div>
<% end %>